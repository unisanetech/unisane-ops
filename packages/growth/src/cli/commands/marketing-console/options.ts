export type MarketingConsoleCliOptions = {
  cwd?: string;
  config?: string;
  json?: boolean;
  out?: string;
  dryRun?: boolean;
  maxAgeDays?: string;
  limits?: string;
  host?: string;
  port?: string;
};
