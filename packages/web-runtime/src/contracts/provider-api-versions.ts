/** Shared pins for independently configured providers. Changes require compatibility evidence. */
export const META_GRAPH_API_VERSION = 'v25.0';
/** Campaign-control compatibility is verified separately from the Graph default. */
export const META_CAMPAIGN_CONTROL_API_VERSION = 'v23.0';
