/** Pinned provider default; changing this requires compatibility and live verification. */
export const META_GRAPH_API_VERSION = 'v25.0';
// Keep the existing campaign-control pin until its compatibility is separately proven.
export const META_CAMPAIGN_CONTROL_API_VERSION = 'v23.0';
